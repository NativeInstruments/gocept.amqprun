# Empty log
<eventlog>
</eventlog>
<amqp-server>
  hostname ${amqp_hostname}
  username ${amqp_username}
  password ${amqp_password}
  virtual_host ${amqp_virtualhost}
</amqp-server>
<worker>
  amount 10
  component-configuration ${site_zcml}
</worker>
<settings>
  test.setting.1 foo
  test.setting.2 bar
  test.setting.unicode Ümläuten
  test.SETTING.__default__ qux
</settings>

