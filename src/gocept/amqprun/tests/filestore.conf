<eventlog>
</eventlog>
<amqp-server>
  hostname ${amqp_hostname}
  username ${amqp_username}
  password ${amqp_password}
  port ${amqp_port}
  virtual_host ${amqp_virtualhost}
</amqp-server>
<settings>
  path /tmp
  routing_key route
</settings>
